Switching to web components was a mistake. See the latest commits.

Let's try another approach to reduce code duplication: askama macros
instead of web components. Those can take positional and optional
arguments and work similarly.

We can use these for badges, buttons, headers, sidebar items, etc.
