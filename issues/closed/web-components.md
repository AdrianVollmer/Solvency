To reduce repeated class names etc, let's leverage web components for common UI
elements. We can write these in TypeScript and hopefully not only reduce the
template size, but also improve visual consistency.
